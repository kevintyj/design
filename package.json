{
	"name": "@design/monorepo",
	"version": "1.0.0",
	"description": "Comprehensive design system with color and spacing generation based on Radix UI colors",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "bun build.ts",
		"build:package": "bun build.ts package",
		"build:clean": "bun build.ts clean",
		"build:list": "bun build.ts list",
		"build:legacy": "bun run --filter='./packages/*' build",
		"dev": "bun run --filter='./packages/*' dev",
		"test": "bun run --filter='./packages/*' test",
		"lint": "bun run --filter='./packages/*' lint",
		"clean": "bun build.ts clean",
		"precommit": "bun run --filter='./packages/*' format",
		"generate": "bun packages/cli/dist/index.js",
		"colors": "bun packages/cli/dist/index.js",
		"colors:quick": "bun packages/cli/dist/index.js quick --output ./output",
		"colors:generate": "bun packages/cli/dist/index.js generate --output ./output",
		"colors:list": "bun packages/cli/dist/index.js list",
		"colors:validate": "bun packages/cli/dist/index.js validate",
		"colors:figma": "bun packages/cli/dist/index.js figma --output ./output/figma/figma-colors.json",
		"spacing:quick": "echo 'Spacing CLI commands coming soon - use spacing packages directly for now'",
		"spacing:generate": "echo 'Spacing CLI commands coming soon - use spacing packages directly for now'",
		"spacing:list": "echo 'Spacing CLI commands coming soon - use spacing packages directly for now'",
		"spacing:validate": "echo 'Spacing CLI commands coming soon - use spacing packages directly for now'",
		"system:quick": "echo 'Combined system CLI commands coming soon - generate colors and spacing separately for now'",
		"system:generate": "echo 'Combined system CLI commands coming soon - generate colors and spacing separately for now'",
		"system:validate": "bun run colors:validate && echo 'Colors: ✅' && echo 'Spacing validation coming soon'",
		"example:basic": "bun packages/examples/src/basic.ts",
		"example:advanced": "bun packages/examples/src/advanced.ts",
		"figma:dev": "bun run --filter='./packages/figma-plugin' dev",
		"figma:build": "bun run --filter='./packages/figma-plugin' build",
		"prepare": "husky"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.1.1",
		"@commitlint/cli": "^19.8.1",
		"@commitlint/config-conventional": "^19.8.1",
		"@types/bun": "^1.2.18",
		"husky": "^9.1.7",
		"typescript": "^5.8.3"
	},
	"engines": {
		"bun": ">=1.0.0"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/your-username/design-system"
	},
	"keywords": [
		"design-system",
		"colors",
		"spacing",
		"radix-ui",
		"css-variables",
		"css-custom-properties",
		"design-tokens",
		"figma",
		"tailwind",
		"monorepo",
		"typescript"
	],
	"author": "Your Name",
	"license": "MIT"
}
