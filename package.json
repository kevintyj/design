{
	"name": "design",
	"description": "Design utils for kevin",
	"author": "Kevin Taeyoon Jin <hello@kevintyj.com>",
	"license": "BSD-3-Clause",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "bun run --watch --hot --env-file=.env.development",
		"start": "bun run --env-file=.env.development",
		"build": "biome check && bun build",
		"lint": "biome check",
		"format": "biome format --write",
		"prepare": "husky",
		"precommit": "biome check --write"
	},
	"devDependencies": {
		"@biomejs/biome": "2.1.1",
		"@commitlint/cli": "^19.8.1",
		"@commitlint/config-conventional": "^19.8.1",
		"@types/bun": "latest",
		"husky": "^9.1.7"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"@radix-ui/colors": "^3.0.0",
		"bezier-easing": "^2.1.0",
		"colorjs.io": "^0.5.2"
	}
}
