{
	"name": "design",
	"description": "Design utils for kevin",
	"author": "Kevin Taeyoon Jin <hello@kevintyj.com>",
	"license": "BSD-3-Clause",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "bun run --watch --hot --env-file=.env.development",
		"start": "bun run --env-file=.env.development",
		"build": "biome check --write --fix && bun build",
		"lint": "biome check",
		"format": "biome format --write"
	},
	"devDependencies": {
		"@biomejs/biome": "2.1.1",
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"@radix-ui/colors": "^3.0.0",
		"bezier-easing": "^2.1.0",
		"colorjs.io": "^0.5.2"
	}
}
